FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

# Create empty .env if not exists (env vars come from docker-compose)
RUN touch .env

EXPOSE 3000

# Run without --env-file since env vars come from docker-compose
CMD ["node", "--experimental-transform-types", "--watch", "src/index.ts"]
